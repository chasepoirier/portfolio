// import api from '../../api'
import { Actions } from './actions'

export const toggleContactForm = (toggle: boolean) =>
  Actions.toggleContactForm(toggle)

export const toggleMobileNav = (toggle: boolean) =>
  Actions.toggleMobileNav(toggle)
